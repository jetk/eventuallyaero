{
  "name": "eventually-aerospace",
  "version": "1.0.0",
  "description": "Eventually Aerospace landing site - static site for Cloudflare Pages",
  "scripts": {
    "build": "tailwindcss -i ./src/input.css -o ./output.css --minify",
    "dev": "tailwindcss -i ./src/input.css -o ./output.css --watch"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.0"
  },
  "keywords": ["static-site", "cloudflare-pages", "tailwindcss"],
  "private": true
}
